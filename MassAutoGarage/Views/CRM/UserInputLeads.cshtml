@model MassAutoGarage.Models.Facebook.LeadsGenerateModel


@{
    ViewBag.Title = "User Input Leads";
    ViewBag.pTitle = "User Input Leads Master";
    ViewBag.pageTitle = "Forms";
    Layout = "~/Views/Shared/_Layout.cshtml";
}


<script src="https://unpkg.com/sweetalert2@7.8.2/dist/sweetalert2.all.js"></script>
<link href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-sweetalert/1.0.1/sweetalert.css" rel="stylesheet" />
@section styles{
    <link href="https://cdn.jsdelivr.net/npm/select2@4.1.0-rc.0/dist/css/select2.min.css" rel="stylesheet" />
}


@using (Html.BeginForm("UserInputLeads", "CRM", FormMethod.Post, new { enctype = "multipart/form-data", OnSubmit = "return ValidationForm()", @class = "form-validate" }))
{


    <div class="row">
        <div class="col-lg-12">
            <div class="card">
                <div class="card-header align-items-center d-flex">
                    <h4 class="card-title mb-0 flex-grow-1">ADD LEADS</h4>
                    <div class="flex-shrink-0">
                        <div class="form-check form-switch form-switch-right form-switch-md">
                            @*<a href="@Url.Action("UserInputLeads","CRM")" class="btn btn-primary" style="float:right;" tabindex="6"><i class="fa fa-list" aria-hidden="true"></i> BACK TO COLOR LIST</a>*@
                        </div>
                    </div>
                </div><!-- end card header -->
                <div class="card-body">
                    <div class="live-preview">
                        <div class="row gy-4">
                            <div class="col-xxl-3 col-md-3">
                                <div>
                                    <label for="labelInput" class="form-label">Lead ID <code>*</code></label>

                                    @Html.TextBoxFor(m => m.ID, new { @class = "form-control", tabindex = 1 })
                                    @Html.ValidationMessageFor(m => m.ID, null, new { @class = "field-validation-error", data_valmsg_for = "ShortName" })
                                </div>
                            </div>
                            <div class="col-xxl-3 col-md-3">
                                <div>
                                    <label for="labelInput" class="form-label">Created DateTime <code>*</code></label>
                                    @Html.TextBoxFor(x => x.Created_Time, new { @class = "form-control", tabindex = 2 })
                                    @Html.HiddenFor(m => m.Created_Time, new { @class = "form-control", })
                                </div>
                            </div>

                            <div class="col-xxl-3 col-md-3">
                                <div>
                                    <label for="labelInput" class="form-label">Ad Id <code>*</code></label>
                                    @Html.TextBoxFor(x => x.Ad_Id, new { @class = "form-control", tabindex = 2 })
                                    @Html.HiddenFor(m => m.Ad_Id, new { @class = "form-control", })
                                </div>
                            </div>
                            <div class="col-xxl-3 col-md-3">
                                <div>
                                    <label for="labelInput" class="form-label">Ad Name <code>*</code></label>
                                    @Html.TextBoxFor(x => x.Ad_Name, new { @class = "form-control", tabindex = 2 })
                                    @Html.HiddenFor(m => m.Ad_Name, new { @class = "form-control", })
                                </div>
                            </div>
                            <div class="col-xxl-3 col-md-3">
                                <div>
                                    <label for="labelInput" class="form-label">AdSet Id <code>*</code></label>
                                    @Html.TextBoxFor(x => x.AdSet_Id, new { @class = "form-control", tabindex = 2 })
                                    @Html.HiddenFor(m => m.AdSet_Id, new { @class = "form-control", })
                                </div>
                            </div>
                            <div class="col-xxl-3 col-md-3">
                                <div>
                                    <label for="labelInput" class="form-label">AdSet Name <code>*</code></label>
                                    @Html.TextBoxFor(x => x.AdSet_Name, new { @class = "form-control", tabindex = 2 })
                                    @Html.HiddenFor(m => m.AdSet_Name, new { @class = "form-control", })
                                </div>
                            </div>
                            <div class="col-xxl-3 col-md-3">
                                <div>
                                    <label for="labelInput" class="form-label">Campaign Id <code>*</code></label>
                                    @Html.TextBoxFor(x => x.Campaign_Id, new { @class = "form-control", tabindex = 2 })
                                    @Html.HiddenFor(m => m.Campaign_Id, new { @class = "form-control", })
                                </div>
                            </div>
                            <div class="col-xxl-3 col-md-3">
                                <div>
                                    <label for="labelInput" class="form-label">Campaign Name<code>*</code></label>
                                    @Html.TextBoxFor(x => x.Campaign_Name, new { @class = "form-control", tabindex = 2 })
                                    @Html.HiddenFor(m => m.Campaign_Name, new { @class = "form-control", })
                                </div>
                            </div>
                            <div class="col-xxl-3 col-md-3">
                                <div>
                                    <label for="labelInput" class="form-label">Form Id<code>*</code></label>
                                    @Html.TextBoxFor(x => x.Form_Id, new { @class = "form-control", tabindex = 2 })
                                    @Html.HiddenFor(m => m.Form_Id, new { @class = "form-control", })
                                </div>
                            </div>
                            <div class="col-xxl-3 col-md-3">
                                <div>
                                    <label for="labelInput" class="form-label">Form Name<code>*</code></label>
                                    @Html.TextBoxFor(x => x.Form_Name, new { @class = "form-control", tabindex = 2 })
                                    @Html.HiddenFor(m => m.Form_Name, new { @class = "form-control", })
                                </div>
                            </div>
                            <div class="col-xxl-3 col-md-3">
                                <div>
                                    <label for="labelInput" class="form-label">Is_organic<code>*</code></label>
                                    @Html.TextBoxFor(x => x.Is_organic, new { @class = "form-control", tabindex = 2 })
                                    @Html.HiddenFor(m => m.Is_organic, new { @class = "form-control", })
                                </div>
                            </div>
                            <div class="col-xxl-3 col-md-3">
                                <div>
                                    <label for="labelInput" class="form-label">Plat form<code>*</code></label>
                                    @Html.TextBoxFor(x => x.Platform, new { @class = "form-control", tabindex = 2 })
                                    @Html.HiddenFor(m => m.Platform, new { @class = "form-control", })
                                </div>
                            </div>
                            <div class="col-xxl-3 col-md-3">
                                <div>
                                    <label for="labelInput" class="form-label">Full Name<code>*</code></label>
                                    @Html.TextBoxFor(x => x.Full_name, new { @class = "form-control", tabindex = 2 })
                                    @Html.HiddenFor(m => m.Full_name, new { @class = "form-control", })
                                </div>
                            </div>
                            <div class="col-xxl-3 col-md-3">
                                <div>
                                    <label for="labelInput" class="form-label">Phone Number<code>*</code></label>
                                    @Html.TextBoxFor(x => x.Phone_number, new { @class = "form-control", tabindex = 2 })
                                    @Html.HiddenFor(m => m.Phone_number, new { @class = "form-control", })
                                </div>
                            </div>
                            <div class="col-xxl-3 col-md-3">
                                <div>
                                    <label for="labelInput" class="form-label">Email<code>*</code></label>
                                    @Html.TextBoxFor(x => x.Email, new { @class = "form-control", tabindex = 2 })
                                    @Html.HiddenFor(m => m.Email, new { @class = "form-control", })
                                </div>
                            </div>
                            <div class="col-xxl-3 col-md-3">
                                <div>
                                    <label for="labelInput" class="form-label">City<code>*</code></label>
                                    @Html.TextBoxFor(x => x.City, new { @class = "form-control", tabindex = 2 })
                                    @Html.HiddenFor(m => m.City, new { @class = "form-control", })
                                </div>
                            </div>


                            <div class="col-xxl-4 col-md-6">
                                <div>

                                </div>
                            </div>
                            @*<div class="col-xxl-4 col-md-6">
                                    <div>
                                        <input id="colorPickerBasic" type="color" />
                                        <input id="colorPickerAdvanced" type="color" />

                                    </div>
                                </div>*@

                            <div class="col-xxl-4 col-md-6">
                                <div>

                                </div>
                            </div>
                            <!--end col-->
                        </div>
                        <!--end row-->
                    </div>

                </div>

                <div class="col-xxl-12 col-md-12">
                    <div class="form-floating" style="float: right; padding-right: 2%;">
                        <a href="#" class="btn btn-danger" tabindex="4">Cancel</a>
                        <input type="button" value="Save" id="btnSave" class="btn btn-primary" tabindex="5" />

                    </div>
                </div>
                <br />
            </div>


        </div>

        <!--end col-->
    </div>
}


@section scripts{


    <!--select2 cdn-->
    <script src="https://cdn.jsdelivr.net/npm/select2@4.1.0-rc.0/dist/js/select2.min.js"></script>

    <script src="~/assets/js/pages/select2.init.js"></script>
    <script src="~/assets/libs/prismjs/prism.js"></script>
    <script src="~/assets/js/app.js"></script>

    <!-- prismjs plugin -->
    <!-- App js -->


}


<script src="~/Scripts/jquery-3.4.1.min.js"></script>
<script src="https://unpkg.com/sweetalert2@7.8.2/dist/sweetalert2.all.js"></script>
<link href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-sweetalert/1.0.1/sweetalert.css" rel="stylesheet" />

<link rel="stylesheet" href="//code.jquery.com/ui/1.12.1/themes/base/jquery-ui.css">
@*<link rel="stylesheet" href="/resources/demos/style.css">*@
<script src="https://code.jquery.com/jquery-1.12.4.js"></script>
<script src="https://code.jquery.com/ui/1.12.1/jquery-ui.js"></script>

<script>
    $(document).ready(function () {


        /* LoadUserType();*/

        //$("#StartDate").datepicker({
        //    format: 'dd/mm/yy',
        //    changeMonth: true,
        //    changeYear: true,
        //    todayHighlight: true,

        //});

        //var options = $.extend({},
        //    $.datepicker.regional["es"], {
        //    dateFormat: "dd/mm/yy",
        //    changeMonth: true,
        //    changeYear: true,
        //    /* yearRange: "-2:+0",*/
        //    todayHighlight: true,
        //    /*highlightWeek: true,*/
        //    /*maxDate: 0*/
        //});
        //$("#StartDate").datepicker(options);

    })

    $("#btnSave112").click(function (e) {
        debugger;
        e.preventDefault();


        var _code = $("#Code").val();
        var _ColorId = $("#ColorId option:selected").val();
        var _ColorCode = $("#ColorCode").val();
        var _IsActive = $("#IsActive option:selected").val();
        var _Id = $("#HDFID").val();
        var _CurrentCount = $("#HDFCurrentCount").val();


        if (_code != "" && _ColorId != "") {
            var ProductListArr = [];
            var ProductList = {
                ID: _Id,
                CurrentCount: _CurrentCount,
                Code: _code,
                ColorId: _ColorId,
                ColorCode: _ColorCode,
                IsActive: _IsActive

            };
            //ProductListArr.push(ProductList);
            if (confirm("Are you sure you want to continue ?")) {
                $.ajax({
                    type: 'POST',
                    url: '/ColorMaster/SaveColorMaster',
                    data: JSON.stringify(ProductList),
                    contentType: "application/json; charset=utf-8",
                    dataType: 'json',
                    success: function (data) {
                        debugger;
                        if (data == "1") {
                            debugger;

                            swal({
                                title: "Success",
                                text: "Records Successfully Saved",
                                type: "success",
                            }).then(function (isConfirm) {
                                if (isConfirm) {
                                    var url = "/ColorMaster/ColorDetails";
                                    window.location.href = url;
                                } else {

                                }
                            });

                        }
                        else if (data == "2") {

                            swal({
                                title: "Success",
                                text: "Records Successfully Updated",
                                type: "success",
                            }).then(function (isConfirm) {
                                if (isConfirm) {
                                    var url = "/ColorMaster/ColorDetails";
                                    window.location.href = url;
                                } else {

                                }
                            });
                        }
                        else if (data == "4") {

                            swal("Warning", "This Color is Already Exist!", "warning");
                        } else {

                            swal("Warning", "Record not save!", "warning");
                        }

                    },
                    error: function (httpResponse) {
                        swal("Warning", "Something Went to Wrong !", "warning");

                    }
                })
            }
        }
        else {


            if (_code == "") {
                swal("Warning", " Please Enter Code !", "warning");
                return false;
            }

            if (_ColorId == "") {
                swal("Warning", "Please Select Color !", "warning");
                return false;
            }


        }

    })


 

</script>


